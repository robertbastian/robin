<!DOCTYPE html>
<html>
<head>
	<title><%= @title || 'Robin' %></title>
		<!-- Bootstrap, legacy
		<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.4/css/bootstrap.min.css">
		<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.4/css/bootstrap-theme.min.css">
		<script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.4/js/bootstrap.min.js"></script>
		-->

		<%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
		<%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
		<%= csrf_meta_tags %>

		<!-- Skeleton -->
		<link rel="stylesheet" href="../../assets/stylesheets/normalize.css">
 		<link rel="stylesheet" href="../../assets/stylesheets/skeleton.css">
		<link rel="stylesheet" href="../../assets/stylesheets/styles.css">

		<!-- FONTS, FONT-AWESOME -->
		<link href='http://fonts.googleapis.com/css?family=Oswald:400,300' rel='stylesheet' type='text/css'>
		<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.3.0/css/font-awesome.min.css">

		<!-- ICO -->
		<link rel="icon" type="image/png" href="images/favicon.ico">

</head>
<body>

  <section id="topbar">
    <div class="container">
      <div class="row">
        <div class="two columns">
          <a href="#"><img src="images/robin_justbird_square.PNG" width="50px" /></a>
        </div>
        <nav class="ten columns">
          <ul>
            <% if logged_in? %>
            <li><a href="#"><i class="fa fa-fw fa-home"></i></a></li>
            <li><a href="#"><i class="fa fa-fw fa-bell"></i></a></li>
            <li><a href="#"><i class="fa fa-fw fa-sign-in"></i></a></li>
            <% else %>
            <li><a href="#"><i class="fa fa-fw fa-sign-out"></i></a></li>
            <% end %>
          </ul>
        </nav>
      </div>
    </div>
  </section>


<!-- 

LEGACY - TO MERGE WITH ABOVE NAV BAR

		<div class="container">
			<nav class="navbar navbar-inverse">
				<div class="container-fluid">
				<div class="navbar-header">
				  <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
				    <span class="sr-only">Toggle navigation</span>
				    <span class="icon-bar"></span>
				    <span class="icon-bar"></span>
				    <span class="icon-bar"></span>
				  </button>
				  <a class="navbar-brand" href="/">Robin</a>
				</div>
				<div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
				  <ul class="nav navbar-nav">
				  	<% if logged_in? %>
				    <li><a href="/dashboard">Dashboard <span class="sr-only">(current)</span></a></li>
				    <li><%= link_to 'Leaderboard', users_url %></li>
				    <li><%= link_to 'Archive', problems_url %></li>
				    <% end %>
				  </ul>
				  <ul class="nav navbar-nav navbar-right">
				    <% if logged_in? %>
				    	<li><%= link_to @current_user.name, user_url(@current_user) %></li>
				    	<li><%= link_to 'Logout', logout_url, method: :post %></li>
				    <% else %>
				    	<li><%= link_to 'Login', login_url %></li>
				    <% end %>
				  </ul>
				</div>
				</div>
			</nav>
			<% if flash[:notice] %>
				<div class='alert alert-info'>
					<%= flash[:notice] %>
				</div>
			<% end; if flash[:danger] %>
				<div class="alert alert-danger">
					<%= flash[:danger] %>
				</div>
			<% end %>
			<%= yield %>
		</div> 


		-->


	   <!--Footer -->
    <section id="footer">
      <div class="container"><div class="row">  
        <!-- columns with social media links and copyrights and that shit here -->
      </div></div>
    </section> 

	</body>
</html>
